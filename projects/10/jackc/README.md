```
$ cargo run -- --help

jackc 0.1.0

s8sato <49983831+s8sato@users.noreply.github.com>

Intermediate program which parse or tokenize `.jack` to `.xml`

USAGE:
    jackc [OPTIONS] <IN_FILE>

ARGS:
    <IN_FILE>    Input `.jack` file

OPTIONS:
    -h, --help        Print help information
    -t, --tokenize    Tokenize instead of parse
    -V, --version     Print version information
```
